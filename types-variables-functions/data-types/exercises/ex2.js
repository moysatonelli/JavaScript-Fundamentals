/*Declare duas variáveis, uma inicializada com valor null e outra que não recebe atribuição (resultando em undefined).
Exiba os valores no console.*/

let nome = null; // é null
let idade; // undefined

console.log(nome,idade);
